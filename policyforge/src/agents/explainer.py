"""Cited natural-language rationale for flagged providers."""

from __future__ import annotations

from src.graph import PolicyForgeState


def explain(state: PolicyForgeState) -> PolicyForgeState:
    """Produce policy-cited explanations for each flag. TODO."""
    return state
